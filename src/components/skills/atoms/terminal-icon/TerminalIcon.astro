---
import terminalIcon from './terminal-icon.module.css';
interface Props {
  name: string;
  src: string;
}
const { name, src } = Astro.props;
---

<div class={terminalIcon.base}>
  <img src={src} alt={name} class={terminalIcon.icon} />
  <span class={terminalIcon.name}>{name}</span>
</div>
